@{
    ViewBag.Title = "About";
    
}

<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3><br />
<h3>@ViewBag.執行時間</h3>
<p>Use this area to provide additional information.</p>

@Html.ActionLink(" ", "Index", null, new { @class ="btn btn-primary", data_name="Vicky"})

<a href="@Url.Action("Index")" class="btn btn-primary " data-name=" Vicky"></a>


<br />
@if (Request.IsLocal) {
    <text>Request.IsLocal</text>
    <div>開發階段好用</div>
}